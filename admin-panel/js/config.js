// Set API_BASE if your backend runs on a different origin (e.g. Backend on :5000, Admin on :5501).
if (typeof window !== "undefined" && !window.API_BASE) {
  // window.API_BASE = "http://localhost:5000";
}
